# Offline repository staging and access verification
Timestamp (Asia/Shanghai): 2026-08-11T12:15:14+08:00
Repository target: controller:/opt/openstack_repo
ZIP SHA256: ddae4b9bd63b20e5ebb052e52d86afb8a98f6edfeefde799f8ccf966aae65978
ZIP size bytes: 726582890
ZIP safety scan: PASS (1089 unique normalized relative entries: 3 directories, 1086 files, 1079 RPMs; no absolute paths, traversal, symlink, or special entries)
repodata/repomd.xml local ZIP-member SHA256: 6ccbb9e00263dd9c35cb93c9c60d83a4e7ace3d9fd4c5c85abf9e5c722978fec
repodata/repomd.xml remote file SHA256:       6ccbb9e00263dd9c35cb93c9c60d83a4e7ace3d9fd4c5c85abf9e5c722978fec

Post-staging complete-tree verification (read-only):
- ZIP normalized entries: 1089 (3 directories, 1086 files).
- controller:/opt/openstack_repo mapped entries: 1089 (3 directories, 1086 files).
- Missing entries: 0.
- Extra entries: 0.
- Type mismatches: 0.
- File-size mismatches across all 1086 non-directory entries: 0.
- repomd SHA256 mismatch: 0.
- Eight core RPM local/remote SHA256 mismatches: 0; individual paths, versions, sizes, and hashes are in rpm-inventory.txt.
- This exact-set comparison includes the seven non-RPM files and excludes contamination from any earlier staging content.
- controller:/opt top-level entries: openstack_repo/ only.
- controller:/opt/.openstack_repo.* residual entries: 0.

Original staging evidence limitation and future procedure:
- The original staging name used only the ZIP SHA256. Calling that path task-unique was inaccurate: a hash-only name can collide across retries or concurrent runs.
- The original run checked that its paths did not exist, but no durable lstat/pre-create evidence was saved in the validation artifacts.
- Future runs must first lstat and canonicalize the exact parent /opt, rejecting a symlink, wrong owner, or any other resolved parent.
- Generate a cryptographically random UUID/task nonce and use an exclusive mkdir (mode 0700) to create /opt/.openstack_repo.task-<uuid>; EEXIST is a hard stop. Immediately lstat the new directory and require a root-owned, non-symlink directory with mode 0700 and the expected initial link count.
- Exclusively create a root-only ownership marker inside that directory containing the UUID/task nonce and ZIP SHA256. The marker is the cleanup authorization token.
- Exclusively create the ZIP inside that directory with mode 0600, using SFTP 'x' only when the implementation also guarantees no-follow semantics; otherwise use a server-side O_CREAT|O_EXCL|O_NOFOLLOW open. Before writing, lstat the new inode and require a regular, non-symlink, root-owned file with nlink=1 and mode 0600; any mismatch or exclusive-create failure is a hard stop. A prior lstat/test ! -e is informational, is not the security boundary, and is not a substitute for exclusive creation.
- Write, close, lstat again, and verify the exact ZIP size and SHA256. Create the extraction child directory with exclusive mkdir, then lstat and validate it before extraction.
- Validate the complete normalized entry set, types, sizes, repomd SHA256, and core RPM hashes before atomic promotion.
- Cleanup is permitted only after revalidating the exact task-directory parent, root ownership/mode, non-symlink state, and ownership-marker UUID/SHA256. Remove only exact children of that verified nonce-bearing directory and remove the directory only when empty.

Controller local DNF source:
[openstack-local]
baseurl=file:///opt/openstack_repo
enabled=1
gpgcheck=0
dnf repolist --disablerepo='*' --enablerepo='openstack-local': PASS
openstack-local  Validated local OpenStack repository

Controller FTP service and exposure:
- vsftpd installed only with --disablerepo='*' --enablerepo='openstack-local' (vsftpd-3.0.5-3.oe2403sp3.x86_64); no dependency RPM was added.
- Service: active. listen=YES; listen_ipv6=NO; anonymous_enable=YES; local_enable=NO; write_enable=NO.
- anon_upload_enable=NO; anon_mkdir_write_enable=NO; anon_other_write_enable=NO; anon_world_readable_only=YES; anon_root=/opt.
- Passive ports: 30000-30009. Active firewalld allows ftp service and 30000-30009/tcp.
- /opt has no readable top-level sibling besides openstack_repo, so the current anonymous FTP root exposes only this repository tree.
- SELinux: Enforcing. No policy or label change was needed.
- Controller FTP request: ftp://192.168.234.151/openstack_repo/repodata/repomd.xml -> Content-Length: 1569, PASS.

Exact backup cleanup evidence:
- Parsed exact path: /root/openstack-repo-backups/vsftpd.conf.pre-openstack-repo-ddae4b9bd63b20e5ebb052e52d86afb8a98f6edfeefde799f8ccf966aae65978.
- Before deletion it was a regular file, size 5039 bytes, SHA256 fa52b7c07f7499930bbd5fdd890b3828d55250bfa39361837944f6236408e0f6.
- Its size and SHA256 show only that its content was identical to etc/vsftpd/vsftpd.conf streamed from the verified offline vsftpd RPM; they do not independently prove creation time or ownership.
- The first execution report contemporaneously recorded creating this exact path immediately before replacing the package configuration. Taken together, that record and the content match are consistent with this being the backup created by this task and supported exact-file cleanup.
- Deleted only that exact file; exact post-delete absence check: PASS. It is recoverable by reinstalling or extracting the same vsftpd RPM.
- /root/openstack-repo-backups is empty but was retained because the original run did not preserve proof that the directory itself was created by this task.

Task 1 baseline comparison:
- Controller RPM delta: added only vsftpd-3.0.5-3.oe2403sp3.x86_64; removed 0; added OpenStack service packages 0.
- Compute RPM delta: added 0; removed 0; added OpenStack service packages 0.
- Compute /dev/sda remains the 200 GiB system disk: sda2 is 192 GiB ext4 mounted at /, and sda3 is swap.
- Compute /dev/sdb remains a 50 GiB whole disk with no child, filesystem, or mount; read-only blkid probe returned no signature.
- Compute /dev/sdc remains a 50 GiB whole disk with no child, filesystem, or mount; read-only blkid probe returned no signature.
- The sda/sdb/sdc mapping, visible signatures, and mount state match the Task 1 baseline. No disk operation was performed.

Compute FTP and DNF source:
[openstack-local]
baseurl=ftp://192.168.234.151/openstack_repo
enabled=1
gpgcheck=0
curl -fsSI ftp://192.168.234.151/openstack_repo/repodata/repomd.xml -> Content-Length: 1569, PASS.
dnf repolist --disablerepo='*' --enablerepo='openstack-local': PASS
openstack-local  Validated controller FTP OpenStack repository
No OpenStack package installation was performed on compute.

Credentials: <REDACTED>

## Lab Task 4B addendum — SELinux policy dependency closure

Timestamp (Asia/Shanghai): 2026-08-11T12:59:18+08:00

This addendum describes the promoted controller repository only. It does not
change or reinterpret the immutable `openstack_repo.zip` inventory above. The
original ZIP remains 726582890 bytes with SHA256
ddae4b9bd63b20e5ebb052e52d86afb8a98f6edfeefde799f8ccf966aae65978
and still contains 1079 RPMs.

Two exact official openEuler 24.03 LTS SP3 `everything` RPMs were overlaid:

- memcached-selinux-1.6.22-4.oe2403sp3.x86_64.rpm
  SHA256 d19f42f77449b9f1d4c600d396d18baa09c959864ad39e8d01969ca62793ec77
- mysql-selinux-1.0.10-1.oe2403sp3.noarch.rpm
  SHA256 079ab9c805f999c3c4c96ac0247a3150ea5e3d34c37330bdc056c471d28cbb95

Both RPM Header V4 RSA/SHA256 signatures and payload SHA256 digests verified
OK against the configured official openEuler key in an isolated rpmdb.

Post-promotion controller repository:

- RPM count: 1081
- repodata/repomd.xml SHA256:
  e5cecca736e5eee2477211225397d1f65d55ae03772ed5224f99634a50a5a3dc
- Eight original core RPM hashes retained: 8/8
- Controller file DNF, controller FTP, and compute FTP DNF: PASS
- Controller and compute available-package counts: 1081 / 1081
- Controller/FTP/compute hashes for repomd and both overlays: MATCH
- Task 5A isolated resolution: Dependencies resolved; Install 163 Packages;
  Removing/Erasing entries 0; operation aborted by `--assumeno`; installed 0
- No new infrastructure or OpenStack service package on either node
- Compute /dev/sda, /dev/sdb, and /dev/sdc state unchanged
- Marker-authorized staging and rollback cleanup: PASS; exact paths absent

The complete before/after, provenance, signature, promotion, cleanup, two-node,
and disk evidence is in `dependency-closure.txt`. Final packaging must use the
tracked `third-edition-work/config/repository-overlay-manifest.json` together
with the ignored `third-edition-work/environment-cache/repository-overlay/`
payloads; the original ZIP must remain unchanged.

### Task 4B reviewer evidence addendum

- Trusted key package: openEuler-gpg-keys-0:1.0-5.4.oe2403sp3.x86_64.
- Trusted key path: /etc/pki/rpm-gpg/RPM-GPG-KEY-openEuler.
- Package/file rpm verification: RC=0 / RC=0, no differences.
- Trusted file and official HTTPS response SHA256:
  006e79d37c10e74c24df6d07c4efc4176515cec009daa5ed493b06f5b6ef39c1.
- Full fingerprint: 8AA16BF9F2CA5244010DCA963B477C60B675600B.
- Overlay issuer long ID: 3B477C60B675600B, matching the fingerprint.
- Complete controller transaction: 163 entries, openstack-local only, all removal classes zero.
- Complete compute transaction: 163 entries, openstack-local only, all removal classes zero.
- Unabridged records: dependency-closure-transaction-controller.txt and
  dependency-closure-transaction-compute.txt.
- Actual swap/rollback/cleanup paths and the contemporaneous-versus-post-hoc
  evidence boundary are recorded in dependency-closure.txt and the Task 4B report.
