# 第12章 智能体的部署和应用

## 学习目标

完成本章学习后，学习者应能够：

1. 区分大语言模型、聊天助手、工作流和智能体；
2. 说明感知、记忆、规划、工具调用、执行、反馈和人工监督之间的关系；
3. 从部署位置、工具权限、模型来源、上下文管理和安全边界等维度比较典型智能体；
4. 在 Windows 教学机上安装 TRAE 国内版，打开 OpenStack 项目工作区；
5. 申请并安全使用 DeepSeek API 密钥，在 TRAE 中完成模型接入和连通性验证；
6. 配置一个用于 OpenStack 部署和运维的专用智能体；
7. 对智能体提出的计划、命令和结果进行人工复核，不以“智能体说成功了”代替证据。

## 12.1 从大语言模型到能执行任务的智能体

大语言模型擅长根据上下文生成文本、代码或结构化内容，但模型本身并不会自动读取本地项目、登录服务器、执行命令或检查服务。聊天助手通常为模型增加了对话界面和有限的上下文管理；工作流把预先确定的步骤连接起来；智能体则在目标和约束下形成计划，选择工具，观察执行结果，并根据反馈继续或停止。

智能体不是“永远正确的自动化程序”。它是一种以模型推理为核心、以工具和环境为行动通道、以反馈和人工监督为闭环的任务执行系统。在 OpenStack 运维场景中，智能体需要同时处理配置文件、命令行、服务状态、API 返回、日志、网络和存储风险，因此“可执行”必须与“可审计”一起设计。

**图12.1 智能体与大语言模型、聊天助手和工作流的关系**  
[绘图待制作：使用四层同心或阶梯结构，最内层为大语言模型，外层依次增加对话、工作流、工具执行与反馈。文件名：`图12.1.svg`、`图12.1.png`。]

表12-2给出四类系统的核心差异。

**表12-2 大语言模型、聊天助手、固定工作流与智能体对比**

| 对比维度 | 大语言模型 | 聊天助手 | 固定工作流 | 智能体 |
| --- | --- | --- | --- | --- |
| 输入 | 提示词与上下文 | 多轮对话 | 结构化参数 | 目标、环境和约束 |
| 步骤 | 生成一次响应 | 对话推进 | 预先编排 | 动态规划和调整 |
| 工具 | 不一定具备 | 有限工具 | 固定调用 | 按任务选择工具 |
| 反馈 | 由用户判断 | 对话反馈 | 按规则分支 | 观察结果并再规划 |
| 风险控制 | 输入输出审查 | 权限与内容审查 | 流程门禁 | 最小权限、人工确认、审计和回滚 |
| 适合本书的用途 | 解释命令 | 辅助答疑 | 固定巡检 | 部署、运维、诊断与报告 |

## 12.2 智能体的基本架构

一个面向云平台的智能体通常包含七个相互配合的部分。

1. **目标与角色**：明确“要完成什么”和“哪些事不能做”。例如，只允许操作教学集群，禁止初始化系统盘。
2. **感知与上下文**：读取用户要求、项目文件、主机状态、配置和日志。上下文越多并不一定越好，应优先提供与当前任务直接相关的材料。
3. **计划**：把复杂目标拆成有依赖关系的步骤。OpenStack 必须先完成基础服务和 Keystone，再部署依赖身份服务的组件。
4. **记忆**：保存当前会话状态、项目规则和已经验证的事实。凭据不应作为普通记忆长期保存。
5. **工具调用**：访问文件、终端、版本库、浏览器或远程主机。工具权限决定了智能体实际能影响的范围。
6. **执行与观察**：运行一步、读取返回码和输出，再决定继续、修正或停止。
7. **人工监督**：在磁盘初始化、资源删除、网络改动和凭据使用等高风险节点由人确认。

**图12.2 云平台智能体“计划—执行—观察—修正”闭环**  
[绘图待制作：标出用户、模型、工具、OpenStack API、两节点环境、证据库和人工审批点。]

智能体的价值不是少敲几条命令，而是把“意图—步骤—证据—责任”连成闭环。好的智能体会在行动前复述关键事实，在行动中保留里程碑，在行动后给出可独立验证的结果；不可靠的智能体则可能跳过前置检查、混淆主机、重复创建资源，或把命令无报错误判为任务成功。

## 12.3 智能体生态与典型产品

截至资料冻结日，智能体已经形成 IDE 内智能体、终端编码智能体、桌面多任务智能体、个人智能体和开源自托管智能体等形态。本书选择 TRAE 与 DeepSeek 作为主要教学组合，同时介绍 Claude Code、OpenAI Codex、OpenClaw 和 Hermes Agent，以帮助学习者理解不同产品的边界，而不是进行简单的“优劣排名”。

### 12.3.1 TRAE 与 DeepSeek：本书的国内教学组合

TRAE 是面向软件开发和工程任务的 AI IDE。官方中文社区在2026年的教程中列出了项目管理、插件管理、自定义智能体、侧边对话、行内对话、Prompt 优化、上下文、MCP、自定义模型和 Memory 等学习内容。本书使用 TRAE 国内版打开 `openstack-ts` 工作区，使智能体能够在项目约束下读取脚本、形成计划并调用经过授权的终端工具。

DeepSeek 在本书中承担模型服务角色。DeepSeek 官方 API 提供兼容常见接口形式的服务入口，并支持工具调用。模型标识可能随服务更新而变化，因此课堂配置前应通过官方“模型列表”或控制台确认可用模型，不把旧模型名写死在长期使用的模板中。

**图12.3 TRAE 国内版官方网站与下载入口**  
[截图待采集：访问 `https://www.trae.cn/ide/download`，保留网址、产品名称和 Windows 下载入口，裁去账号信息。]

**图12.4 DeepSeek 开放平台与 API 文档入口**  
[截图待采集：访问 DeepSeek 官方开放平台和 API 文档，展示 API 文档导航，不显示账户余额与密钥。]

### 12.3.2 Claude Code

Claude Code 是 Anthropic 提供的终端型编码智能体。它以项目目录和命令行为主要工作环境，可阅读代码、修改文件、执行命令并接入 MCP 等工具。其优势是与终端工程流程结合紧密；使用时同样需要控制目录范围、命令权限和凭据暴露。教材只做生态简介，不把它作为本篇实验主工具。

**图12.5 Claude Code 官方文档中的终端工作界面**  
[截图待采集：仅使用 Anthropic 官方文档或官方产品截图，标注来源与访问日期，不截取第三方博客。]

### 12.3.3 OpenAI Codex

OpenAI Codex 是面向真实项目任务的智能体系统，可在应用、终端、IDE 和云端等形态中工作。官方资料强调项目上下文、任务执行、并行协作、终端记录、测试结果和可配置的安全边界。对云平台教学而言，它体现了“让多个任务可监督地推进”的方向。教材介绍其工作形态与安全理念，不要求学生购买或登录该产品。

**图12.6 OpenAI Codex 官方应用界面**  
[截图待采集：使用 OpenAI 官方产品页或官方文档中的界面，展示任务列表、工作区或验证结果；不得包含个人任务。]

### 12.3.4 OpenClaw

OpenClaw 是开源的个人智能体项目，强调在用户自己的设备上运行，可通过网关连接多种消息通道，并通过 Skills、工具和模型提供商扩展能力。它适合用于理解“模型、网关、通道、技能和本地执行环境”如何组合。自托管不等于天然安全，开放工具、浏览器、消息通道或远程执行前仍需最小权限、认证和审计。

**图12.7 OpenClaw 官方项目首页与体系结构**  
[截图待采集：来源 `https://github.com/openclaw/openclaw` 或 `https://docs.openclaw.ai/`，展示项目名称和核心组成。]

### 12.3.5 Hermes Agent

Hermes Agent 是 Nous Research 的开源智能体项目。官方项目将其描述为具有工具、记忆和技能能力的可扩展智能体，并提供终端、服务端和桌面等使用形态。它适合用于讨论长期记忆、技能沉淀、多模型接入和远程运行，但实际部署必须明确单用户信任边界、凭据保存位置和终端工具的影响范围。

**图12.8 Hermes Agent 官方项目页或桌面界面**  
[截图待采集：仅使用 Nous Research 官方 GitHub 仓库或官方文档图片，标注开源许可证与访问日期。]

表12-3从教学角度比较六个对象。

**表12-3 本章六个智能体相关对象的教学定位**

| 对象 | 主要形态 | 模型与工具关系 | 本书定位 |
| --- | --- | --- | --- |
| TRAE | AI IDE | 在工作区中使用模型、终端和上下文工具 | 主实验平台 |
| DeepSeek | 模型/API 服务 | 通过 API 为智能体提供推理与工具调用能力 | 主实验模型 |
| Claude Code | 终端编码智能体 | 终端、项目文件、MCP 等 | 国外生态案例 |
| OpenAI Codex | 应用、终端、IDE、云端智能体 | 项目、工具、测试与多任务协作 | 国外生态案例 |
| OpenClaw | 开源个人智能体 | 网关、通道、Skills、模型与本地工具 | 开源生态案例 |
| Hermes Agent | 开源多形态智能体 | 记忆、技能、工具和多模型提供商 | 开源生态案例 |

## 12.4 安装 TRAE 国内版

### 12.4.1 安装前准备

教学机建议采用 Windows 11 64 位系统，具备稳定网络、可用磁盘空间和受控的管理员权限。实验前准备：

- 第三版教材项目副本，路径中包含 `openstack-ts`；
- 能够访问 TRAE 国内版官网和 DeepSeek 开放平台；
- 两台教学虚拟机已经按第4章规划网络和磁盘；
- 一个专门用于教学的 DeepSeek API 密钥；
- 截图目录和脱敏规范。

先记录下载日期、安装包版本和 SHA-256。版本号必须来自安装包属性或“帮助—关于”，不能根据网页外观推测。

**图12.9 TRAE 安装包下载与版本信息**  
[截图待采集：包含下载入口和文件名；另在图注记录 SHA-256，截图不显示浏览器账号。]

### 12.4.2 完成安装与首次启动

1. 从 TRAE 国内版官方下载页获取与教学机体系结构一致的安装包。
2. 双击安装包，阅读许可与隐私条款，确认安装路径。
3. 完成安装后启动 TRAE，按界面提示完成登录或初始化。
4. 如出现导入其他编辑器设置的选项，教学环境建议先使用默认配置，减少扩展和快捷键差异。
5. 打开“帮助—关于”，记录 TRAE 版本、系统版本和实验日期。

**图12.10 TRAE 安装向导**  
[截图待采集：显示安装路径和主要选项，隐藏 Windows 用户名。]

**图12.11 TRAE 首次启动与登录界面**  
[截图待采集：暂停输入密码和验证码时的截图；登录完成后再拍，遮挡头像和账号。]

### 12.4.3 打开 OpenStack 工作区

在 TRAE 中选择“打开文件夹”或界面中的等效入口，打开第三版教材工作副本中的 `openstack-ts` 目录。确认左侧文件树能看到 `final-scripts`、部署说明和远程执行工具。不要直接打开包含其他个人项目的上级目录，以免扩大智能体可见范围。

工作区打开后完成三项检查：

1. 当前根目录确实是受控脚本副本，不是用户原始备份；
2. 终端默认目录位于该工作区；
3. 搜索 `192.168.234.151`、`192.168.234.150`、`/dev/sdb` 和 `/dev/sdc` 能定位到对应配置。

**图12.12 TRAE 中打开 openstack-ts 工作区**  
[截图待采集：展示文件树、编辑区和受控副本路径；不得显示真实密码。]

表12-4说明本实验需要认识的界面区域，具体名称以实际版本为准。

**表12-4 TRAE 实验界面区域与使用边界**

| 区域 | 用途 | 使用边界 |
| --- | --- | --- |
| 文件树 | 查看脚本、文档和配置 | 只打开教材项目副本 |
| 编辑区 | 阅读与修改提示词、脚本 | 修改前比较差异 |
| 智能体/对话区 | 输入任务、查看计划与反馈 | 不粘贴密钥和密码 |
| 终端区 | 执行本地或远程命令 | 高风险命令需人工确认 |
| 问题/日志区 | 查看错误、返回码和测试结果 | 不只截最后一行“成功” |
| 模型设置 | 选择提供商、模型和参数 | 密钥只在遮蔽控件中输入 |

## 12.5 在 TRAE 中接入 DeepSeek

### 12.5.1 申请并保护 API 密钥

登录 DeepSeek 开放平台，在 API 密钥管理页面新建教学专用密钥。密钥通常只在创建时完整显示，应立即放入系统凭据管理器或 TRAE 的安全密钥输入控件。不要把它写入 Markdown、脚本、`.env` 示例、聊天记录、命令历史或截图。

建议为教学密钥设置独立用途、预算和轮换周期。若平台支持额度或使用量查看，应在课前检查余额；若密钥疑似泄露，立即撤销并新建，不要仅删除截图。

**图12.13 DeepSeek API 密钥创建入口**  
[截图待采集：只显示“创建密钥”入口；创建与复制密钥期间暂停截图和录屏，完成后确认列表仅显示掩码。]

### 12.5.2 添加模型提供商

打开 TRAE 的模型或 AI 功能设置，选择“自定义模型”“OpenAI 兼容提供商”或当前版本中的等效入口。按表12-5填写。若界面不支持自定义提供商，则先检查 TRAE 官方“自定义模型”教程和当前版本功能，不通过修改未知内部文件绕过产品限制。

**表12-5 DeepSeek 自定义模型字段**

| 字段 | 教学填写值 | 说明 |
| --- | --- | --- |
| 提供商名称 | `DeepSeek-Teaching` | 便于与内置模型区分 |
| Base URL | `https://api.deepseek.com` | 以 DeepSeek 官方文档为准 |
| API Key | 在遮蔽控件中现场输入 | 不进入截图和教材 |
| 模型 ID | 从官方模型列表选择可用 ID | 不长期写死已退役名称 |
| 超时/重试 | 保持默认，按课堂网络调整 | 避免无限重试产生费用 |

资料冻结日的官方文档列出了 `deepseek-v4-pro` 和 `deepseek-v4-flash` 等模型标识。课堂上应先调用官方模型列表或查看控制台，再选择实际可用项；如果 TRAE 的下拉列表与文档不同，以 API 返回和 TRAE 官方兼容说明为准。

**图12.14 TRAE 自定义模型与 DeepSeek 参数**  
[截图待采集：完整显示提供商、Base URL 和模型 ID；API Key 输入框必须完全遮挡，截图后进行二次 OCR 检查。]

### 12.5.3 进行最小连通性测试

不要一开始就请求部署。先发送一个不使用终端、不修改文件的测试提示词：

```text
请只读取当前工作区的文件名，不执行命令、不修改文件。
用三行回答：
1. 当前模型标识；
2. 是否看到 final-scripts 目录；
3. 你将如何避免显示 API 密钥。
```

检查模型指示、响应完整性和工具状态。连通成功只说明模型可调用，不说明它已获得远程主机权限，更不说明 OpenStack 可以部署。

**图12.15 DeepSeek 模型连通性测试结果**  
[截图待采集：保留模型标识、提示词和三行结果；裁去会话账号。]

## 12.6 创建 OpenStack 部署与运维智能体

新建自定义智能体，名称建议为“OpenStack 教学平台智能体”。将工作区限制为受控 `openstack-ts` 副本，开放只需的文件读取、终端和版本差异查看能力。默认采用“先计划、后确认、再执行”；磁盘初始化、网络地址修改和删除资源必须再次确认。

可使用下面的角色说明作为系统提示词基础：

```text
你是 OpenStack 教学平台智能体。目标是在两台 openEuler 24.03 LTS SP3
教学虚拟机上，依据当前工作区 openstack-ts 的已验证脚本，部署并运维
OpenStack 2023.1 Antelope。

固定事实：controller=192.168.234.151，compute=192.168.234.150；
管理网卡 ens33；Provider 映射网卡 ens34 且不得配置 IP；
compute 的 /dev/sda 是系统盘，严禁初始化；/dev/sdb 仅用于 Cinder；
/dev/sdc 仅用于 Swift，两盘均为 50 GiB 教学盘。

行动规则：先读取说明和脚本，输出环境事实、风险、阶段顺序、验证和回滚点；
未经人工批准不得执行。凭据只通过交互或受保护的运行时环境获取，不得显示、
记录或写入项目。每一阶段检查返回码；失败立即停止，不跳过、不掩盖。
部署后必须用 OpenStack CLI/API 和服务状态独立验收，不以自然语言结论代替证据。
删除操作只按本任务创建的精确资源 ID 执行，禁止模糊匹配和越界清理。
```

**图12.16 OpenStack 专用智能体的角色和工具设置**  
[截图待采集：显示智能体名称、模型、工作区和权限；可把角色说明与权限设置拼成同号双面板图。]

### 12.6.1 权限分级

表12-6给出建议的权限分级。

**表12-6 OpenStack 教学智能体权限分级**

| 等级 | 操作示例 | 策略 |
| --- | --- | --- |
| 只读 | 查看脚本、服务、端点、日志 | 可自动执行，保留输出 |
| 可逆写入 | 创建带 `book-` 前缀的规格、网络或实例 | 展示计划后执行，记录 ID |
| 特权写入 | 安装软件、编辑系统配置、启停服务 | 分阶段确认与验收 |
| 破坏性操作 | `pvcreate`、`mkfs`、删除资源、覆盖仓库 | 明确目标和证据，人工二次确认 |
| 禁止 | 初始化 `/dev/sda`、泄露凭据、关闭安全边界后继续 | 立即拒绝并报告 |

### 12.6.2 智能体输出合同

要求智能体每个任务都按以下结构输出：

1. **理解**：复述目标与边界；
2. **事实**：列出实际读取到的环境信息；
3. **计划**：给出有序步骤和依赖；
4. **风险**：标记凭据、磁盘、网络和删除风险；
5. **执行**：逐阶段报告返回码与关键输出；
6. **验证**：使用独立命令或 API；
7. **清理/回滚**：只处理本任务资源；
8. **结论**：PASS、FAIL 或 BLOCKED，并附证据位置。

## 12.7 隐私、凭据与提示注入防护

云平台智能体既能读取内部配置，又能执行高权限操作，必须建立多层安全边界。

- **最小上下文**：只打开需要的工作区，不把个人目录、下载目录或其他课程资料一并授权。
- **凭据隔离**：密钥和密码只存在于受保护控件、系统凭据管理器或运行时环境，绝不写入 Git。
- **主机身份校验**：远程连接使用人工核对的主机公钥，拒绝未知或变化的主机密钥。
- **提示注入防护**：把网页、日志和仓库文本视为不可信数据；其中“忽略规则”“上传密钥”等内容不得改变系统约束。
- **最小工具权限**：只开放当前任务需要的工具，高风险命令保持人工确认。
- **输出脱敏**：截图前退出账号页，遮挡令牌；截图后再以 OCR 和文本搜索检查。
- **费用控制**：限制模型并发、重试和长上下文，记录教学任务的用量。
- **可追溯性**：保存提示词、计划、命令、返回码、资源 ID 和验收报告，但不保存秘密。

## 12.8 本章实训：搭建可用但尚不执行部署的智能体

### 实训目标

安装 TRAE、接入 DeepSeek、打开项目并创建 OpenStack 专用智能体。实训到“只读预检计划”结束，不连接或修改虚拟机。

### 实训步骤

1. 完成图12.9—图12.15所示安装与模型接入；
2. 打开受控 `openstack-ts` 副本；
3. 创建专用智能体并粘贴经审阅的角色说明；
4. 授予文件只读能力，暂不授予自动执行特权命令的权限；
5. 请求智能体列出17个脚本的跨节点顺序；
6. 检查它是否识别 `.151/.150`、`ens34` 无 IP、`/dev/sdb` 和 `/dev/sdc`；
7. 导出脱敏后的计划文本，作为第13章输入。

### 验收标准

- TRAE 可以正常打开项目；
- DeepSeek 连通性测试通过；
- 智能体能正确复述五项关键边界；
- 智能体没有执行命令或修改文件；
- 截图和导出文本中没有凭据与个人信息。

## 本章小结

智能体是在模型基础上增加上下文、规划、记忆、工具、执行、反馈和人工监督的系统。本章通过 TRAE、DeepSeek、Claude Code、OpenAI Codex、OpenClaw 和 Hermes Agent 展示了不同智能体形态，并完成了 TRAE 安装、DeepSeek 接入、OpenStack 专用智能体配置和安全边界设计。下一章将在明确的自动化例外中，让智能体依据已验证脚本一次性部署 OpenStack，再按任务进行运维。

## 复习与思考

1. 为什么“能生成正确命令”不等于“能安全完成运维任务”？
2. 工作流和智能体在步骤确定性方面有什么区别？
3. 为什么 API 密钥不能写进项目的 `.env` 示例并提交？
4. 智能体读取日志时可能遇到哪些提示注入风险？
5. 设计一个只允许读取 OpenStack 状态、不允许修改资源的权限方案。

---

