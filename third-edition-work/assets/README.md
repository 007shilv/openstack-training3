# 第三版图片工作区

最终独立图片目录为工作区根目录外的 `第三版教材图片/`。本目录保存可重复生成图片所需的登记表和工具，不在Git中重复保存第二版原稿的521个媒体文件。

- `_第二版候选原图/`：从第二版DOCX只读导出的候选图，仅用于筛选；大部分分辨率低或界面过时，不直接进入第三版。
- 新技术架构图优先重绘为SVG，并输出同编号高清PNG。
- 软件界面截图采用无损PNG；真实账号、API Key、密码、Token、Cookie和个人信息必须遮挡。
- 图号按章连续，如`图1.1`；表号按章连续，如`表1-1`。
