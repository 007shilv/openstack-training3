# 第十一章 Cinder的安装及其配置

本章导读：Cinder为OpenStack提供块存储服务。它把计算节点上的独立磁盘、卷组和逻辑卷组织为可由项目申请的云硬盘，并向Nova提供卷连接信息。实例系统盘可以来自镜像临时生成，也可以放在Cinder卷中长期保存；数据卷则可在实例之间按权限连接和分离。

本书在compute节点使用一块50 GiB独立磁盘/dev/sdb构建LVM后端。controller运行Cinder API和调度服务，compute运行卷服务和LIO目标服务。该结构便于观察控制请求、卷调度与数据通道之间的区别。

图11.1表示Cinder控制面和数据面的分工。控制节点处理身份认证、API请求、调度和卷状态，计算节点使用LVM（Logical Volume Manager，逻辑卷管理器）创建逻辑卷，并通过iSCSI（Internet Small Computer Systems Interface，互联网小型计算机系统接口）向实例提供块设备。

{{FIGURE:图11.1}}

图11.1中，用户请求先到达cinder-api，cinder-scheduler根据后端容量和状态选择cinder-volume。卷服务调用LVM创建逻辑卷，再由LIO（Linux I/O Target，Linux输入输出目标框架）建立目标。实例连接卷时，数据在计算节点与存储后端之间传输，不经过cinder-api。

## 11.1 Cinder功能与组件

一．块存储对象

1．卷、快照与镜像

卷是Cinder管理的基本块设备，可以作为实例的数据盘，也可以保存可启动系统。快照记录卷在某一时刻的状态，可用于创建新卷。镜像与卷之间可以互相转换，但镜像服务管理的是镜像对象，Cinder管理的是可读写块设备，两者的存储语义不同。

2．卷类型与后端

卷类型用于把用户请求映射到特定存储能力。一个云平台可以同时连接LVM、分布式存储和存储阵列，并用卷类型区分性能、可靠性或介质。本书只有一个LVM后端，仍创建名为lvm的卷类型，以建立服务目录、卷类型和后端名称之间的对应关系。

二．Cinder服务组件

1．API与调度服务

cinder-api接收卷的创建、查询、删除和连接请求，并把状态写入数据库。cinder-scheduler根据容量统计、卷类型和过滤规则选择后端。两项服务部署在controller，与Keystone、MariaDB和RabbitMQ共同构成控制面。

2．卷服务与目标服务

cinder-volume部署在持有数据盘的compute节点。它根据配置调用LVM驱动，在cinder-volumes卷组中创建逻辑卷，并通过LIO iSCSI目标驱动公布连接信息。targetclid负责LIO目标配置，因此应先启动targetclid，再启动cinder-volume。

表11-1归纳主要组件的部署位置和作用。

{{TABLE:表11-1}}

## 11.2 LVM后端与卷访问过程

一．LVM存储结构

1．物理卷与卷组

物理卷PV（Physical Volume，物理卷）把/dev/sdb纳入LVM管理，卷组VG（Volume Group，卷组）把物理卷空间汇集为cinder-volumes。Cinder在卷组中建立逻辑卷LV（Logical Volume，逻辑卷），每个逻辑卷对应一个云硬盘对象。

2．逻辑卷与精简池

LVM后端可以使用精简置备，让逻辑容量按需映射到底层空间。精简置备提高磁盘利用率，但仍需监视实际占用，避免逻辑容量总和远大于物理空间后出现写入失败。实验环境采用单盘结构用于学习对象关系，生产环境通常使用具有冗余、监控和备份能力的共享存储。

二．卷连接过程

图11.2表示实例连接Cinder卷时的主要路径。Nova向Cinder取得连接信息，计算节点根据iSCSI目标信息建立会话，再把块设备交给虚拟机。数据读写由虚拟机、主机内核和存储后端共同完成。

{{FIGURE:图11.2}}

图11.2中的控制请求用于创建卷和取得连接参数，数据请求用于实际读写。把两条路径分开，有助于判断“卷对象已经创建但实例无法访问”的故障：前者多与API、数据库和调度有关，后者多与目标服务、网络和后端设备有关。

表11-2列出本章使用的存储参数。

{{TABLE:表11-2}}

## 11.3 卷调度、连接与状态变化

Cinder把不同厂商、不同协议和不同性能的存储后端统一为卷、快照、备份和卷类型等对象。统一API并没有消除后端差异，Scheduler必须根据卷请求与后端能力选择位置，Volume服务必须通过驱动完成真实操作，Nova还要与Cinder共同完成卷和计算主机之间的连接。

一．卷创建与后端选择

1．请求与初始状态

用户创建卷时提交容量、卷类型、可用区、源镜像或源快照等参数。cinder-api验证身份、策略、配额和参数，随后在数据库建立卷记录，状态通常从creating开始。数据库记录先于后端资源出现，使各服务能够围绕同一个卷ID协调工作。

卷类型（Volume Type，描述一类卷服务特征的对象）通过extra specs表达后端名称、性能等级、加密或多连接等要求。本书建立lvm卷类型，并用`volume_backend_name=LVM`把它与compute上的LVM后端对应。名称相同不是自动匹配，真正关系由extra specs和后端上报能力决定。

cinder-volume周期性上报后端容量、可用区、驱动能力和运行状态。Scheduler根据这些事实构造BackendState。一个后端只有同时满足服务在线、可用区、容量、卷类型和能力要求，才会通过过滤。没有后端通过时，卷进入error，存储设备上不应出现一个可用卷。

2．过滤与称重

Cinder过滤（Filtering，排除不满足硬条件的后端）通常先检查可用区和容量，再结合卷类型extra specs、驱动能力和一致性组等条件。过滤后的后端都“可以创建”，但仍可能有不同剩余容量、负载和位置。

称重（Weighing，对可用后端排序）为每个通过过滤的后端计算权重。默认容量称重可以倾向剩余空间较多的后端，也可以通过配置改变分布策略。称重只在通过硬条件的后端之间比较，不能让容量不足的后端因为其他分数较高而重新进入候选。

Scheduler选定后端后，通过消息队列把任务交给相应cinder-volume。Volume服务调用配置的驱动，在LVM、Ceph、SAN或其他后端创建真实资源。驱动成功后更新卷状态为available；驱动返回错误时更新为error，并记录能够定位后端阶段的信息。

本书单一LVM后端不会呈现复杂的多后端选择，但仍保留API、Scheduler、Volume和驱动的完整分工。这样将来增加共享存储时，只需扩展后端和卷类型，不需要改变用户创建卷的基本接口。

二．Attachment连接对象

1．预留与初始化连接

Attachment（卷连接记录，保存卷与实例连接关系的对象）把“准备连接”和“完成连接”分开。Nova还没有把实例任务交给具体compute时，可以先为卷和实例建立Attachment，预留这次连接。此时卷从available进入reserved或attaching相关过渡状态，避免不支持多连接的卷同时被其他实例占用。

目标计算主机确定后，nova-compute向Cinder提交Connector（连接器，描述主机存储连接能力的信息）。Connector可以包含主机名、发起端标识、网络地址和支持的协议。cinder-volume根据驱动和Connector准备目标，并返回连接信息。

本书LVM后端通过LIO建立iSCSI目标。返回信息包括目标地址、目标IQN、逻辑单元号和访问方式。nova-compute调用宿主机存储连接工具发现目标，把块设备连接到主机，再把设备加入Libvirt实例定义。Cinder提供连接参数，真正的设备发现和虚拟机挂载发生在计算节点。

设备连接完成后，Nova通知Cinder完成Attachment，卷状态进入in-use。若目标准备成功但宿主机连接失败，需要终止后端连接并清理Attachment，不能只把数据库状态改回available，否则后端可能仍保留访问关系。

2．分离与异常恢复

分离过程采用相反顺序。Nova先使来宾操作系统停止使用设备，并从Libvirt定义中移除磁盘；Cinder把卷置为detaching，cinder-volume终止目标连接；最后删除Attachment并把卷恢复为available。任何一层仍在使用设备时，都不应强制跳到最终状态。

Attachment记录使多连接卷能够分别跟踪每个实例和主机的连接。普通卷通常只允许一个有效Attachment，多连接卷则依赖后端能力和文件系统或应用的一致性设计。支持多个连接不等于多个主机可以无协调地同时写入任意文件系统。

异常分离可能在计算主机留下设备，也可能在存储后端留下会话。遗留设备被新任务误用会造成数据泄漏或损坏。处理这类问题时，应同时核对Nova实例磁盘、Cinder Attachment、宿主机连接和后端目标，不能只看卷列表的一列状态。

三．卷状态与数据边界

1．创建和使用状态

creating表示后端创建尚未完成，available表示卷可用于连接，attaching和detaching表示连接关系正在变化，in-use表示至少存在一个完成的连接，deleting表示正在删除，error表示某项操作失败。快照、备份和迁移还有各自的状态，不能用卷状态替代所有存储对象状态。

卷从镜像创建时，后端先建立卷，再把镜像数据写入卷。这个过程可能比空卷创建更长，状态也可能经历downloading等阶段。只有最终进入available，用户才能把它作为普通持久卷连接。源镜像损坏和后端容量不足会落在不同失败位置。

删除卷前需要确认没有有效Attachment和依赖快照。删除成功后，后端空间与数据库对象才共同释放。绕过Cinder直接删除LVM逻辑卷会使数据库仍然认为卷存在；直接删除数据库记录则可能在后端留下数据。控制面和数据面必须通过驱动过程一起改变。

2．精简置备与容量

LVM精简池允许卷的逻辑容量总和超过当前已经写入的物理数据量，提高空间利用率。逻辑容量不是已经占用的物理容量。随着实例写入，精简池的数据和元数据空间不断增长，若物理池耗尽，多个卷可能同时受到影响。

Scheduler依据后端上报的容量和超分配置决定还能否接收新卷。超分不是无成本增加磁盘，而是基于“不是所有卷都会同时写满”的容量管理策略。生产平台必须持续监控物理使用量并提前扩容，不能只看已创建卷的数量。

3．卷类型与后端能力

卷类型（Volume Type，块存储服务对后端能力的分类）把用户请求与具体存储能力联系起来。类型的额外规格可以指定后端名称、存储协议、性能等级或驱动公开的能力。Scheduler先用这些条件过滤不匹配的后端，再在剩余后端之间称重。用户选择的是稳定的服务分类，不需要知道某个逻辑卷或存储池的内部名称。

卷类型不会改变已经创建卷的真实后端。修改类型定义主要影响后续调度，已有卷若要迁移到其他后端，需要由Cinder执行迁移或重新创建过程。快照和由快照创建的新卷还要考虑源卷所在后端是否支持相应能力。把后端能力通过类型公开，可以在保持接口一致的同时区分容量型、性能型和特殊功能存储。

图11.3表示卷创建、后端选择和实例连接的主要分支。没有后端通过过滤时卷进入error；后端创建成功后进入available。连接阶段若目标初始化或宿主机连接失败，则执行终止连接和Attachment清理；成功后进入in-use。分离完成后回到available，可以再次连接。

{{FIGURE:图11.3}}

图11.3中的回环体现卷的可重复使用：available卷可以连接到实例，正常分离后再次回到available。错误分支必须经过资源清理，不能越过连接终止过程直接修改最终状态。

表11-3汇总卷生命周期中的主要状态、负责服务和允许的后续操作。

{{TABLE:表11-3}}

表11-3说明同一卷操作会跨越API、Scheduler、Volume、Nova和计算主机。状态停留在哪一阶段，就应先检查负责该阶段的服务和数据路径，再判断是否需要清理连接或后端资源。

## 11.4 实训项目8 Cinder的手工安装与配置

一．实训目标

1．完成Cinder数据库、服务用户、服务目录和端点的创建。

2．完成controller节点Cinder配置、数据库同步和控制服务启动。

3．确认compute节点/dev/sdb为50 GiB独立数据盘，并建立cinder-volumes卷组。

4．完成compute节点LVM后端、LIO目标服务和cinder-volume服务的配置。

二．创建数据库和身份对象

1．创建数据库

在controller进入MariaDB，创建cinder数据库，并为三个连接来源分别建立同名账户。实验环境统一使用口令qwer1234。

```command
[root@controller ~]# mysql -u root -p
Enter password: qwer1234
MariaDB [(none)]> CREATE DATABASE cinder;
MariaDB [(none)]> CREATE USER 'cinder'@'localhost' IDENTIFIED BY 'qwer1234';
MariaDB [(none)]> CREATE USER 'cinder'@'127.0.0.1' IDENTIFIED BY 'qwer1234';
MariaDB [(none)]> CREATE USER 'cinder'@'%' IDENTIFIED BY 'qwer1234';
MariaDB [(none)]> GRANT ALL PRIVILEGES ON cinder.* TO 'cinder'@'localhost';
MariaDB [(none)]> GRANT ALL PRIVILEGES ON cinder.* TO 'cinder'@'127.0.0.1';
MariaDB [(none)]> GRANT ALL PRIVILEGES ON cinder.* TO 'cinder'@'%';
MariaDB [(none)]> FLUSH PRIVILEGES;
MariaDB [(none)]> EXIT;
```

2．创建服务用户、服务和端点

载入管理员环境变量，创建cinder用户，把它加入service项目并授予admin角色。随后创建volumev3服务和RegionOne中的三个端点。

```command
[root@controller ~]# source /root/admin-openrc
[root@controller ~]# openstack user create --domain Default --password qwer1234 cinder
[root@controller ~]# openstack role add --project service --user cinder admin
[root@controller ~]# openstack service create --name cinderv3 --description "OpenStack Block Storage" volumev3
[root@controller ~]# openstack endpoint create --region RegionOne volumev3 public "http://controller:8776/v3/%(project_id)s"
[root@controller ~]# openstack endpoint create --region RegionOne volumev3 internal "http://controller:8776/v3/%(project_id)s"
[root@controller ~]# openstack endpoint create --region RegionOne volumev3 admin "http://controller:8776/v3/%(project_id)s"
```

三．配置controller节点

1．安装软件包并编辑配置

```command
[root@controller ~]# dnf -y install openstack-cinder
[root@controller ~]# vi /etc/cinder/cinder.conf
```

在cinder.conf中写入以下参数。消息队列、数据库和服务用户口令均为qwer1234。

```config
[DEFAULT]
transport_url = rabbit://openstack:qwer1234@controller
auth_strategy = keystone
my_ip = 192.168.234.151

[database]
connection = mysql+pymysql://cinder:qwer1234@127.0.0.1/cinder

[keystone_authtoken]
www_authenticate_uri = http://controller:5000
auth_url = http://controller:5000
memcached_servers = controller:11211
auth_type = password
project_domain_name = Default
user_domain_name = Default
project_name = service
username = cinder
password = qwer1234

[oslo_concurrency]
lock_path = /var/lib/cinder/tmp
```

2．同步数据库并启动服务

```command
[root@controller ~]# su -s /bin/sh -c "cinder-manage db sync" cinder
[root@controller ~]# systemctl enable openstack-cinder-api openstack-cinder-scheduler
[root@controller ~]# systemctl start openstack-cinder-api openstack-cinder-scheduler
```

在Nova配置中声明Cinder所在区域，然后重启nova-api使设置生效。

```command
[root@controller ~]# vi /etc/nova/nova.conf
```

```config
[cinder]
os_region_name = RegionOne
```

```command
[root@controller ~]# systemctl restart openstack-nova-api
```

四．配置compute节点存储后端

1．确认并初始化/dev/sdb

先查看块设备、挂载点和LVM信息，确认/dev/sda是系统盘、/dev/sdb是50 GiB独立空白盘、/dev/sdc保留给对象存储。

```command
[root@compute ~]# lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINTS
[root@compute ~]# findmnt -n -o SOURCE /
[root@compute ~]# pvs -o pv_name,vg_name
[root@compute ~]# wipefs --no-act /dev/sdb
[root@compute ~]# blkid -p /dev/sdb
```

只有/dev/sdb确认为50 GiB独立空白盘时，才输入YES并建立物理卷和卷组。

```command
[root@compute ~]# read -p "确认初始化/dev/sdb，输入YES：" ANSWER
[root@compute ~]# test "$ANSWER" = "YES"
[root@compute ~]# pvcreate /dev/sdb
[root@compute ~]# vgcreate cinder-volumes /dev/sdb
```

2．安装软件包并编辑配置

```command
[root@compute ~]# dnf -y install lvm2 device-mapper-persistent-data targetcli openstack-cinder-volume
[root@compute ~]# vi /etc/lvm/lvm.conf
```

在devices段保留系统盘和Cinder数据盘，避免LVM扫描Swift使用的/dev/sdc。

```config
devices {
    filter = [ "a/sda/", "a/sdb/", "r/.*/" ]
}
```

```command
[root@compute ~]# vi /etc/cinder/cinder.conf
```

```config
[DEFAULT]
transport_url = rabbit://openstack:qwer1234@controller
auth_strategy = keystone
my_ip = 192.168.234.150
enabled_backends = lvm
glance_api_servers = http://controller:9292

[database]
connection = mysql+pymysql://cinder:qwer1234@controller/cinder

[keystone_authtoken]
www_authenticate_uri = http://controller:5000
auth_url = http://controller:5000
memcached_servers = controller:11211
auth_type = password
project_domain_name = Default
user_domain_name = Default
project_name = service
username = cinder
password = qwer1234

[lvm]
volume_driver = cinder.volume.drivers.lvm.LVMVolumeDriver
volume_group = cinder-volumes
target_protocol = iscsi
target_helper = lioadm
volume_backend_name = LVM

[oslo_concurrency]
lock_path = /var/lib/cinder/tmp
```

3．启动目标服务和卷服务

目标服务负责向计算节点提供块设备访问目标，卷服务负责管理LVM后端并响应控制节点下发的卷任务。先启动targetclid，再启动openstack-cinder-volume，可以使卷服务在建立iSCSI连接信息时使用已经就绪的LIO目标环境。

```command
[root@compute ~]# systemctl enable targetclid openstack-cinder-volume
[root@compute ~]# systemctl start targetclid
[root@compute ~]# systemctl start openstack-cinder-volume
```

五．创建卷类型

在controller载入管理员环境变量，创建lvm卷类型，并把它与配置中的LVM后端名称关联。

```command
[root@controller ~]# source /root/admin-openrc
[root@controller ~]# openstack volume type create lvm
[root@controller ~]# openstack volume type set --property volume_backend_name=LVM lvm
```
